import 'package:flutter/material.dart';

class AMobileAppDeveloperWidget extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Text(
      'A Mobile App Developer',
      style: TextStyle(fontSize: 16),
    );
  }
}
